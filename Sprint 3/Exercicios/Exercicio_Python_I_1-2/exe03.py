# Declarando a variável 'numeros' com a lista de números pares de 0 até 20
numeros = list(range(0, 21, 2))  # A função range() vai de 0 a 20 com passo 2, ou seja, só pares

# Imprimindo cada número da lista 'numeros'
for numero in numeros:
    print(numero)
