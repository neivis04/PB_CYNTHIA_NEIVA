# Lista fornecida
a = [1, 4, 9, 16, 25, 36, 49, 64, 81, 100]

# Gerando uma nova lista contendo apenas números ímpares
impares = [num for num in a if num % 2 != 0]

# Imprimindo a nova lista
print(impares)
